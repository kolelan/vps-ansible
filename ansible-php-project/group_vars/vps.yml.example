---
# Подключение: paramiko + ключ (см. inventory: ansible_ssh_private_key_file).
# При необходимости входа по паролю задайте VPS_SSH_PASSWORD — скрипт scripts/wsl-ansible.sh
# для ansible-playbook передаст -e ansible_password (см. Readme-vps.md).
ansible_connection: paramiko
