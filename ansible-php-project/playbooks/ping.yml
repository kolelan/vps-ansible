---
# Проверка SSH и Python на VPS
# ansible-playbook playbooks/ping.yml
- name: Проверка связи с VPS
  hosts: vps
  connection: paramiko
  gather_facts: false
  tasks:
    - name: Ping
      ansible.builtin.ping:
