# Установка только через python3 -m pip (в активированном venv):
#   python3 -m pip install -r requirements.txt
ansible>=9
# Вход по паролю без sshpass на control node (WSL/Linux):
paramiko>=3.4
